describe('FileManager', () => {
  it('should exist', () => {
    expect(new rjc.FileService()).toBeDefined();
  });

  it('should exist', () => {
    expect(new rjc.FileService()).toBeDefined();
  });
});
